# API Configuration
PORT=5000
NODE_ENV=development

# Firebase Admin SDK Credentials
# You need to get a Firebase Service Account JSON from:
# Firebase Console → Project Settings → Service Accounts → Generate New Private Key
# Then paste the JSON content here OR set individual variables below:

# Option 1: Paste the entire firebase-admin credentials JSON
FIREBASE_SERVICE_ACCOUNT_JSON={}

# Option 2: Or set individual environment variables
FIREBASE_PROJECT_ID=studio-3437465512-f56c8
FIREBASE_PRIVATE_KEY_ID=your_private_key_id
FIREBASE_PRIVATE_KEY=your_private_key
FIREBASE_CLIENT_EMAIL=firebase-adminsdk@your-project.iam.gserviceaccount.com
FIREBASE_CLIENT_ID=your_client_id
FIREBASE_AUTH_URI=https://accounts.google.com/o/oauth2/auth
FIREBASE_TOKEN_URI=https://oauth2.googleapis.com/token
FIREBASE_AUTH_PROVIDER_X509_CERT_URL=https://www.googleapis.com/oauth2/v1/certs
FIREBASE_CLIENT_X509_CERT_URL=your_cert_url

# CORS Settings
CORS_ORIGIN=http://localhost:5173

# Frontend URL (for password reset links and redirects)
FRONTEND_URL=http://localhost:5173

# =============================
# GMAIL SMTP (for Email Features)
# =============================
# Required for: Forgot Password emails, TOTP confirmation emails
# 
# Setup instructions:
# 1. Enable 2-Factor Authentication on your Gmail account
# 2. Go to: https://myaccount.google.com/apppasswords
# 3. Select "Mail" and "Windows Computer" (or your OS)
# 4. Google generates a 16-character app password
# 5. Copy it below (you can remove the spaces)
#
# IMPORTANT: Use an app password, NOT your Gmail password
# IMPORTANT: Keep these credentials SECRET - never commit to git

GMAIL_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=xxxx xxxx xxxx xxxx
